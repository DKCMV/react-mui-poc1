import {Container, Typography} from '@mui/material';
import "../App.css";

const Header = () => {
    return(
        <Container>
            <Typography variant="h1" color="primary"> Villen</Typography>
        </Container>
    );
}

export default Header;